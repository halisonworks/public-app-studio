# Trip Budget

> ⚠️ **Example, not a product.** This project was generated as a starting point and learning aid. It may contain mistakes, simplified models, or inaccurate numbers — do not rely on any calculation or result for real decisions without checking it yourself.

![Trip Budget screenshot](./screenshot.png)

Plan a trip's total cost and see it broken down **per person** and **per day**.
Add expenses in categories, mark each as per-person and/or per-day, set the
number of travellers and days, and watch the totals and category breakdown
update live. Everything is saved to your browser.

A single self-contained HTML page (Tailwind via CDN). Category: **Travel** (personal). **English UI.**

## Load it into App Studio

- **Load it straight in** — use [`trip-budget.appstudio`](./trip-budget.appstudio):
  **Share → Import**, or drop it into your `App Studio Projects` folder.
- **Copy-paste** — open [`trip-budget.html`](./trip-budget.html), copy it all,
  then **New → paste → Run App**.

## Notes

- Private and single-user; data is stored in your browser's `localStorage`.
  **No runtime service dependency** — all maths is computed locally.
- Keep the header comment and the source link at the top of the file. See the
  root [LICENSE](../../../LICENSE) for the terms.
